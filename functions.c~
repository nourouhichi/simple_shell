#include <cstring>

/**
 * _strlen - returns the length of a string.
 * @s : variable.
 * Return: n.
 */

int _strlen(char *s)
{
int n = 0;
int i;
for (i = 0 ; s[i] != 0 ; i++)
{
n++;
}
return (n);
}
